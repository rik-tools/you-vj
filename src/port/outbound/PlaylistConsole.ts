
import PlaylistIdentity from '../../type/domain/PlaylistIdentity.ts';

type PlaylistConsole = {
    printPlaylists (playlistIdentities: PlaylistIdentity []): void;
}

export default PlaylistConsole;
